/**
 * STUB — wires to modules/tenants/service.ts (registerTenant, setPersonality,
 * goLive) and app/api/v1/catalog/import for step 4. Build order per PRD §7:
 * 1. Register workspace  2. Choose AI personality  3. Connect systems
 * 4. Sync & index        5. Sandbox test           6. Go live
 */
export default function OnboardingPage() {
  return (
    <main className="min-h-screen bg-bg flex items-center justify-center">
      <p className="text-text-dim">Onboarding wizard — steps 1–6 per PRD §7, not yet built.</p>
    </main>
  );
}
